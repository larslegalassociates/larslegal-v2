import {notFound} from "next/navigation";import Link from "next/link";import {practices} from "../../data";
export function generateStaticParams(){return practices.map(([slug])=>({slug}))}
export function generateMetadata({params}){const p=practices.find(x=>x[0]===params.slug);return {title:p?.[1]||"Practice Area"}}
export default function Page({params}){const p=practices.find(x=>x[0]===params.slug);if(!p)return notFound();return <main><section className="pageHero"><div className="container"><div className="eyebrow">Practice Area</div><h1>{p[1]}</h1><p>{p[2]}</p></div></section><section className="section white"><div className="container split"><div><div className="eyebrow">How We Assist</div><h2>Clear advice. Focused strategy.</h2><p>{p[2]}</p><p>Our team identifies the legal and practical issues, explains available options and develops a strategy suited to the client’s objectives. The exact scope depends on the facts, applicable law and professional requirements of the relevant jurisdiction.</p><div className="notice">General information only. This page does not constitute legal advice.</div></div><div className="card"><h3>Discuss your matter</h3><p>For advice tailored to your circumstances, request a consultation.</p><Link className="btn dark" href="/contact">Book Consultation</Link></div></div></section></main>}
